# Skill: Planning Checklist

Always append indexing update tasks and verify dependencies.

Minimum checklist:
- Ensure plan follows spec -> plan -> tasks -> implement.
- Append final `Index Update` task assigned to `indexing`.
- Include dependency-aware ordering and parallel-safe groupings.
- Add continuity mini-plan when quota threshold warnings are active.
