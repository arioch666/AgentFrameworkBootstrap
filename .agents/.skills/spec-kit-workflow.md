# Skill: Spec Kit Workflow

Use this skill to enforce spec -> plan -> tasks -> implement.

Real-time checks:
- Warn when implementation is requested before spec/plan/tasks are present.
- Warn when documentation changes bypass spec/task lifecycle.
- Suggest the next best command/prompt that restores canonical flow.
